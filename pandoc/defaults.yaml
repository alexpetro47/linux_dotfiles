# PANDOC DEFAULTS

reader: gfm+hard_line_breaks+strikeout+task_lists+smart
writer: pdf

standalone: true
pdf-engine: pdflatex

wrap: preserve
columns: 72

filters:
  - /home/alexpetro/.config/pandoc/highlight-filter.lua
  - mermaid-filter

highlight-style: tango

metadata:
  mermaidFilter:
    theme: dark
    width: 1200
    format: svg
    background: transparent
  geometry: margin=0.75in
  fontsize: 11pt
  documentclass: article
  papersize: letter
  colorlinks: true
  linkcolor: blue
  urlcolor: blue
  mainfont: "DejaVu Serif"
  sansfont: "DejaVu Sans"
  monofont: "DejaVu Sans Mono"
  pagestyle: plain
  listings: true

variables:
  geometry: margin=0.75in
  fontsize: 11pt

include-in-header:
  - /home/alexpetro/.config/pandoc/highlight-header.tex
